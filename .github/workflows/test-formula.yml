name: Test formula

on:
  push:
    branches: [main]
    paths: ["Formula/**", ".github/workflows/test-formula.yml"]
  workflow_dispatch:

jobs:
  install:
    name: brew install (macOS)
    runs-on: macos-latest
    steps:
      - name: Tap this repository
        run: |
          TAP_DIR="$(brew --repository)/Library/Taps/hevn-inc/homebrew-tap"
          mkdir -p "$(dirname "$TAP_DIR")"
          git clone --depth 1 --branch "${GITHUB_REF_NAME}" "https://github.com/${GITHUB_REPOSITORY}.git" "$TAP_DIR"

      - name: Install formula
        run: brew install hevn-inc/tap/hevn-cli

      # `brew test` is skipped: on GitHub runners it wrongly reports the
      # formula's poured dependencies (python@3.12, sqlite) as missing. The
      # smoke test below covers what matters: the installed binary runs.
      - name: Smoke-test binary
        run: |
          hevn --version
          hevn --help
